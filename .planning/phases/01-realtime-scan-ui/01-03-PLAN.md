---
phase: 01-realtime-scan-ui
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/extensions/dashboard/Dashboard.tsx
autonomous: false
requirements:
  - SCAN-01
  - SCAN-02
  - SCAN-03
  - SCAN-04
  - SCAN-05

must_haves:
  truths:
    - "Health rings visually animate to their final score per-repo while other repos are still scanning"
    - "Diagnostic breakdown (pass/fail per rule) is visible per repo card during the scan"
    - "Shimmer skeleton appears on repo cards whose scan is in-flight and disappears when results arrive"
    - "Health ring animation is smooth 300ms ease-in-out (no jump)"
    - "Navigating away and back during or after a scan does not cause duplicate event processing"
  artifacts:
    - path: "src/extensions/dashboard/Dashboard.tsx"
      provides: "Complete wired UI with all scan UI features verified visually"
  key_links:
    - from: "src/extensions/dashboard/Dashboard.tsx RepoRow"
      to: "useDiagnosticsStore"
      via: "scanningRepos.has() and reports[fullName] selectors"
      pattern: "scanningRepos\\.has"
---

<objective>
Verify the complete realtime scan UI works end-to-end with visual confirmation.

Purpose: All five SCAN requirements must be verified as working together. Plans 01 and 02 built the plumbing; this plan confirms the integrated experience meets the phase success criteria.

Output: Visual confirmation that all scan UI features work correctly.
</objective>

<execution_context>
@/Users/phmatray/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phmatray/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-realtime-scan-ui/01-RESEARCH.md
@.planning/phases/01-realtime-scan-ui/01-01-SUMMARY.md
@.planning/phases/01-realtime-scan-ui/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration smoke test — build and type-check the full stack</name>
  <files>src/extensions/dashboard/Dashboard.tsx</files>
  <action>
1. Run `cargo build --manifest-path src-tauri/Cargo.toml` to verify Rust compiles with the extended ScanProgress struct.
2. Run `npx tsc --noEmit` to verify TypeScript compiles with updated bindings, scan.slice.ts, and Dashboard.tsx.
3. Run `npm run check` (Biome lint/format) to ensure code style is consistent.
4. If any errors are found, fix them. Common issues to watch for:
   - Missing `useCallback` import in Dashboard.tsx
   - `Set<string>` type issues in scan.slice.ts (Zustand state)
   - Null check on `event.payload.report` before accessing `.repoFullName`
   - bindings.ts `ScanProgress` type not matching the Rust struct fields

5. If Dashboard.tsx has any inline diagnostic breakdown rendering that was missed in Plan 02 (e.g., showing pass/fail counts or rule names per repo card), ensure it is wired to the `report` from the per-repo selector. The existing repo card shows `report.healthScore` via HealthRing and issue counts — verify the rule-level breakdown (criticalCount, warningCount, infoCount) is also displayed per card.

If the repo card does NOT currently show diagnostic breakdown inline (only the health ring score), add a small indicator below the repo description showing the counts:
```typescript
{report && (report.criticalCount > 0 || report.warningCount > 0) && (
  <div className="flex items-center gap-2 mt-0.5">
    {report.criticalCount > 0 && (
      <span className="font-mono text-[10px] text-error">{report.criticalCount} critical</span>
    )}
    {report.warningCount > 0 && (
      <span className="font-mono text-[10px] text-warning">{report.warningCount} warning</span>
    )}
  </div>
)}
```
This satisfies SCAN-02 — diagnostic breakdown appears immediately per repo during scan.
  </action>
  <verify>
    <automated>cd /Users/phmatray/Repositories/github-phm/GitHubAutomate && cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -3 && npx tsc --noEmit 2>&1 | tail -3 && npm run check 2>&1 | tail -5</automated>
  </verify>
  <done>Full stack compiles: Rust build succeeds, TypeScript type-check passes, Biome lint/format clean. Diagnostic breakdown counts are visible per repo card.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all SCAN requirements in running app</name>
  <files>src/extensions/dashboard/Dashboard.tsx</files>
  <action>
Run `npm run tauri dev` to start the app. Authenticate with GitHub if not already authenticated. Navigate to the Dashboard Repositories tab and trigger a full scan.

Verify all 5 SCAN requirements visually:
- SCAN-01: Health rings fill per-repo DURING the scan
- SCAN-02: Critical/warning counts appear per card after each repo completes
- SCAN-03: Shimmer skeleton on in-flight repo cards
- SCAN-04: Smooth 300ms health ring animation
- SCAN-05: HMR reload during scan does not cause duplicate processing

If any requirement fails, describe the issue for remediation.
  </action>
  <verify>
    <automated>cd /Users/phmatray/Repositories/github-phm/GitHubAutomate && npm run tauri dev 2>&1 | head -5</automated>
  </verify>
  <done>All 5 SCAN requirements visually confirmed working in the running Tauri app. User has approved.</done>
</task>

</tasks>

<verification>
1. Cargo build succeeds
2. TypeScript type-check passes
3. Biome lint passes
4. Visual verification of all 5 SCAN requirements by human
</verification>

<success_criteria>
- All 5 SCAN requirements verified working in the running app
- No TypeScript or Rust compilation errors
- No visible layout shift when skeleton transitions to health ring
- Health ring animation is smooth 300ms, not a jump
- No console errors related to duplicate event listeners
</success_criteria>

<output>
After completion, create `.planning/phases/01-realtime-scan-ui/01-03-SUMMARY.md`
</output>
